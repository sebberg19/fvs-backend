PORT=3000
NODE_ENV=production
STRIPE_SECRET_KEY=sk_live_xxx
RETURN_BASE=https://futbolero.netlify.app
STRIPE_WEBHOOK_SECRET=

# SMTP (optional) — if omitted, email will be logged but not sent
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Futbolero Shop <no-reply@exemple.com>"

# Notification target (shop owner)
ORDER_NOTIFY_TO=futbolerovintageshop@gmail.com
